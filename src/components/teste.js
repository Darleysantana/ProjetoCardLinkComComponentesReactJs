function StringChallenge(str) { 
    str = "ana";
 
   inverse = str.split('').reverse().join('');
 
   if(inverse === str && str === String)
   {
     inverte;
     str = true;
   }
   else
   {
     str = false;
   }
 
   // code goes here  
   return str; 
 
 }
    
 // keep this function call here 
 console.log(StringChallenge( inverse + readline()));